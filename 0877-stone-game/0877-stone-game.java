class Solution {
    public boolean stoneGame(int[] piles) {
        // Alice can always win because the number of piles is even
        // and she can choose either all even-indexed or odd-indexed piles.
        return true;
    }
}